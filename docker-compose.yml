services:
  web:
    image: ghcr.io/adrianmfuentes/martinezfuentesadrian.dev:latest
    restart: unless-stopped
    ports:
      - "3001:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
